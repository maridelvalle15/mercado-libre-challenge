import React from "react";

const Breadcrumb = ({ categories }) => {
  const renderCategories = ()=>{
    categories.map(category => <Typography></Typography> )
  }
  return <div></div>;
};

export default Breadcrumb;
